[project]
name = "msd-backtest"
version = "0.1.0"
description = "MSD backtest framework"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
